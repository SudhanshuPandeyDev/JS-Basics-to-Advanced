// Showing how to do in ES Modules (ESM) (Node.js)
// JS Module: Exporting Addition, Subtraction, and Multiplication Fns.

export function add(a, b) {
  return a + b;
}

export function sub(a, b) {
  return a - b;
}

export default function mul(a, b) {
  return a * b;
}
